# Coverage: the landscape we mapped, and the subset that generates

_Generated by `make coverage` from `docs/landscape.yaml`. Never hand-edited. The
`simulated` block is asserted equal to `afl/attack/templates/vectors.yaml` on every
build, so this document cannot claim a vector the engines cannot produce._

**26 vectors identified, 8 fully simulated.** M1 runs in template mode:
valid traffic of roughly the right shape, without the defining tell. The rest are
specified, not generated.

Identified means the vector and the surface it would need are specified. These are
demonstrated or specified capabilities, not necessarily mass-exploited patterns. This
document makes no claim about how common any of them is in the field: prevalence is a
separate question, and we did not measure it.

## What generates today

| id | vector | level | engine | tier | status |
|---|---|---|---|---|---|
| `S1` | Mule network & layering | mechanism | graph | strong | built |
| `S2` | Card testing / BIN enumeration | mechanism | velocity | strong | built |
| `S3` | Account takeover via drift | mechanism | drift | strong | built |
| `C1` | Bust-out | mechanism | drift | common | built |
| `C2` | UPI collect-request / APP scam | enabler | velocity | common | built |
| `C3` | Instant-A2A pass-through | mechanism | graph | common | built |
| `M1` | Boundary probing / paced evasion | model-attack | velocity | mid | **template** |
| `M2` | Synthetic-identity lifecycle | enabler | drift | mid | built |
| `M3` | First-party / friendly fraud | mechanism | drift | mid | built |

## What is specified but not generated

### Mechanism: how the money moves

| id | vector | surface | why not built |
|---|---|---|---|
| `ME-STRUCT` | Adaptive structuring / smurfing under thresholds | bank_transfer | no anchor for this surface |
| `ME-XRAIL` | Cross-rail hop (card -> wallet -> bank -> crypto) | multi_rail | no anchor for this surface |
| `ME-PEEL` | Peel-chain / structured crypto off-ramp | crypto | no anchor for this surface |

### Enabler: what is forged before the transaction

| id | vector | surface | why not built |
|---|---|---|---|
| `EN-KYCVID` | Deepfake liveness / KYC video-injection bypass | onboarding | no anchor for this surface |
| `EN-VOICE` | Voice-clone vishing (CEO fraud, family-emergency) | social_eng_voice | no anchor for this surface |
| `EN-VIDEO` | Deepfake video-call authorising a wire (Arup-style) | social_eng_video | no anchor for this surface |
| `EN-ROMANCE` | LLM-automated pig-butchering / romance scam at scale | social_eng_app | no anchor for this surface |
| `EN-PHISH` | Breach-personalised spear-phishing at scale | account_takeover | no anchor for this surface |
| `EN-PAGE` | Adaptive phishing pages that mutate to evade URL/DOM | account_takeover | no anchor for this surface |
| `EN-OTPBOT` | AI voice-bot OTP interception mid-auth | account_takeover | no anchor for this surface |
| `EN-DISPUTE` | LLM-automated chargeback / friendly-fraud disputes | disputes | no anchor for this surface |
| `EN-MERCH` | Synthetic merchant / fake acquiring for bust-out | acquiring | no anchor for this surface |

### Model attack: against the detector, not the victim

| id | vector | surface | why not built |
|---|---|---|---|
| `MO-POISON` | Feedback-loop data poisoning of the retrained detector | pipeline | no anchor for this surface |
| `MO-EXTRACT` | Model extraction / query-based boundary recon | detector_api | no anchor for this surface |
| `MO-SHAP` | SHAP-guided feature manipulation (gradient-free) | detector | no anchor for this surface |
| `MO-INJECT` | Prompt injection against a payment LLM copilot | agentic | no anchor for this surface |
| `MO-AGENT` | Agentic-commerce abuse (autonomous purchasing agents) | agentic | no anchor for this surface |

## Why the gap is what it is

Every vector above needs an anchor that carries its surface. The four public anchors we
tried are transaction ledgers: they carry payments, not onboarding documents, not voice
calls, not liveness video, not dispute narratives. A vector whose tell lives outside the
transaction stream cannot be simulated against them honestly, and generating it anyway
would produce rows whose only distinguishing feature is that we wrote them.

That is the same reasoning the commensurability audit applies per batch, one level up.

## Standalone audits

| vector | separability | worst field | verdict |
|---|---|---|---|
| `summary.json` | - | - | - |
