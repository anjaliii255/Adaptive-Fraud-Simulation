# Fidelity, on the real anchor

Generated by `make fidelity` from `artifacts/fidelity/*.json`. Every number below traces to one
of those files; nothing here is typed by hand.

Three levels and a privacy panel. **They are not equal.** Level 3 asks whether training on the
generated data teaches a detector anything about real fraud, and it is the gate. Levels 1 and 2
ask whether the traffic resembles the anchor, and they are diagnostics: they explain why level 3
landed where it did, and they can neither fail a generator nor rescue one. The headline score is
capped at the level-3 score for that reason — a reader who quotes the number instead of the
verdict still cannot be handed a pass two sets of histograms averaged into existence.

| anchor | verdict | level 1 | level 2 | level 3 (**gate**) | privacy |
| --- | --- | --- | --- | --- | --- |
| amlsim | ❌ **FAIL** — score 0.25 | 0.7672 | 0.228 | 0.25 | 0.6488 |
| paysim | ❌ **FAIL** — score 0.0 | 0.7495 | 0.2054 | 0.0 | 0.7763 |

## amlsim

❌ **FAIL** — score 0.25

1,323,234 real rows (1,719 fraud) against 635 generated fraud rows over 9 vectors, at fixed FPR
0.01 and k=100.

**Why:**

- TSTR gap 0.404137 exceeds the bar 0.15
- membership is inferable from the synthetic data alone
- level 2 score 0.228 below 0.6 (worst: reciprocity)
- the generator's standalone output trains a detector to 0.000897, below the amount floor 0.455644 — reported, not gating

| system | trained on | rows | PR-AUC | recall@FPR | p@k | beats the floor |
| --- | --- | --- | --- | --- | --- | --- |
| trtr | real rows, real labels | 930,465 | 1.0 | 1.0 | 1.0 | yes |
| tstr | real legit + generated fraud, no real fraud label | 929,930 | 0.595863 | 0.630515 | 0.96 | yes |
| augmented | real rows + generated fraud | 931,100 | 0.991541 | 1.0 | 0.99 | yes |
| amount_floor | nothing | 0 | 0.455644 | 0.474265 | 1.0 | — |
| standalone | the generator's whole output, background included | 19,496 | 0.000897 | 0.0 | 0.0 | **no** |

Levels 1 and 2 above compare the generated fraud with the anchor's own fraud. The same two
levels on the whole batch against the whole anchor land at 0.6178 and 0.3984; an anchored run
discards the simulator's background and injects the attacks into real traffic, so this
comparison is mostly about a haystack nothing downstream uses.

Privacy, as evidence rather than proof: DCR ratio 3.453889 over 1 dropped constant column(s),
exact duplicates of training rows 0.0, membership-inference advantage 0.35119 against 0.343365
for the same attack between two halves of the holdout, where nothing was ever in training —
leaving 0.007825 that is about membership rather than about the calendar. 1.0 of generated rows
name an account that exists in the anchor, which is the envelope working as designed and is
reported rather than flagged.

Thresholds: the bars were committed in 6989a9ef9848 on 2026-08-22 and every value still matches
that commit; this run started 2026-08-26T11:43:22+00:00.

Artefact: `artifacts/fidelity/amlsim.json` · `artifacts/fidelity/amlsim.md`

## paysim

❌ **FAIL** — score 0.0

636,409 real rows (803 fraud) against 635 generated fraud rows over 9 vectors, at fixed FPR 0.01
and k=100.

**Why:**

- TSTR gap 0.153009 exceeds the bar 0.15
- held-out recall lift -0.229268 below 0.0 — the data does not help
- TSTR PR-AUC 0.004856 loses to the amount floor 0.057261 — training on this data is worse than sorting the test window by amount
- membership is inferable from the synthetic data alone
- level 2 score 0.2054 below 0.6 (worst: fan_in_share)
- the generator's standalone output trains a detector to 0.003195, below the amount floor 0.057261 — reported, not gating

| system | trained on | rows | PR-AUC | recall@FPR | p@k | beats the floor |
| --- | --- | --- | --- | --- | --- | --- |
| trtr | real rows, real labels | 446,214 | 0.157865 | 0.443902 | 0.53 | yes |
| tstr | real legit + generated fraud, no real fraud label | 446,480 | 0.004856 | 0.02439 | 0.0 | **no** |
| augmented | real rows + generated fraud | 446,849 | 0.047476 | 0.214634 | 0.14 | **no** |
| amount_floor | nothing | 0 | 0.057261 | 0.212195 | 0.23 | — |
| standalone | the generator's whole output, background included | 19,496 | 0.003195 | 0.0 | 0.0 | **no** |

Levels 1 and 2 above compare the generated fraud with the anchor's own fraud. The same two
levels on the whole batch against the whole anchor land at 0.6264 and 0.1814; an anchored run
discards the simulator's background and injects the attacks into real traffic, so this
comparison is mostly about a haystack nothing downstream uses.

Privacy, as evidence rather than proof: DCR ratio 2.431659 over 3 dropped constant column(s),
exact duplicates of training rows 0.0, membership-inference advantage 0.223726 against 0.079803
for the same attack between two halves of the holdout, where nothing was ever in training —
leaving 0.143923 that is about membership rather than about the calendar. 1.0 of generated rows
name an account that exists in the anchor, which is the envelope working as designed and is
reported rather than flagged.

Thresholds: the bars were committed in 6989a9ef9848 on 2026-08-22 and every value still matches
that commit; this run started 2026-08-26T11:43:22+00:00.

Artefact: `artifacts/fidelity/paysim.json` · `artifacts/fidelity/paysim.md`

## Not measured

These anchors have a data config and no scorecard. An anchor that is absent from a table reads
as one that passed, so each gets a row and a reason.

- **amlworld** — no scorecard in the artefact directory
- **banksim** — no scorecard in the artefact directory

## What this does not claim

Levels 1 and 2 are diagnostics, not proofs of realism. A distance near zero means the statistics
we chose to measure agree; it does not mean a fraud analyst would be fooled, and it never means
the data is safe to publish.

DCR and MIA are evidence against memorisation, not a privacy guarantee. They say the two leakage
paths we tested for are not present. A formal claim needs differential privacy, and this project
does not make one — see the identifier-reuse line in each panel for the disclosure path neither
of them can see.

A failing scorecard stays failed. The artefacts are written before this script exits non-zero,
and the thresholds carry an origin commit that every run reads back out of git, so a bar that
moves after a result exists is visible in the artefact rather than in a blame trail.
