# Leave-one-attack-out

_Generated by `scripts/build_loao.py` from the anchors on disk. Every number below traces to
`artifacts/loao/<anchor>.json`, which carries the eval config, the committed split digest and the
seed that produced it. Do not edit this file — re-run `make loao`._

Train without one attack family, then measure recall on that family alone. Reporting recall on a
family the model trained on measures memorisation; this is the only fold that asks about
generalisation to an unseen attack, and everything downstream of ticket 11 is measured through
it.

**Operating point: recall at 1% FPR, precision@100**, fixed
in `config/eval/leave_one_attack_out.yaml` before any of these numbers existed.

## The three guards

They are assertions in `afl/evaluation/leave_one_attack_out.py`, not intentions, and each has a
test that deliberately tries to leak a row past it.

1. **Not one row of the held-out family reaches training — replay buffer included.** The audit
   runs against the *fitted detector*, via its `training_rows`, not against the list handed to
   `fit`. The replay buffer is the place a carved-out family walks back into training four
   rounds later without the split changing. A detector that cannot say what it trained on fails
   the guard rather than passing it.
2. **The split is still out-of-time, with the committed embargo intact after the carve-out.**
   Removing a family shortens both sides and the arithmetic says the gap can only widen — but an
   argument is exactly what an assertion replaces.
3. **Every legit row of the test window stays in the holdout.** An FPR measured without
   negatives is not an FPR, and precision@100 on a holdout of 100 positives is 1.0 by
   construction.

A fourth check is not a guard but a verdict, and it is the one that decides most of this table.
The carve-out drops the anchor's own fraud from the holdout, so **every positive in a fold is an
injected row and every negative is a real one** — "caught the fraud" and "spotted the synthetic
row" are the same label. The provenance probe asks a classifier to make exactly that call on the
fold's own features. Where it succeeds, the fold's recall is a statement about the generator, and
the number is withheld. Ticket 07 measured this by hand and got AUC 1.00 on PaySim; it lives in
the harness now so it cannot be quoted around.

Read the probe asymmetrically. It learns "injected" from the fold's own positives, so a **high**
probe score is strong evidence the fold is measuring provenance, and a **low** one on a fold with
few positives is weak evidence of anything — the probe simply had less to learn from than the
detector it is checking. The positive count sits beside every probe score for that reason.

## What a fold is allowed to claim

A fold that runs is not a fold that means something. Each row is one of three outcomes, and only
the first carries a claim:

- **measured** — the numbers stand.
- **withheld** — the fold ran and the numbers exist, under `withheld_metrics` rather than
  `metrics`, but nothing may be concluded from them. Four ways to land here: fewer than
  30 positives, so recall moves further per row than the
  differences anyone would read into it; the family is separable from the anchor by a single
  contract field; **a classifier can sort the injected rows from the anchor's own**, so the fold
  is measuring provenance rather than detection; or the vector is a `template` whose defining
  tell is not modelled yet.
- **skipped** — the fold never ran, and the reason sits in the cell where the number would be.

**4 of 18 folds across 2 anchor(s) carry a quotable number.** Read a fold's provenance probe
before quoting its recall: the two are the same measurement pointed at different questions.

**What the measured rows say next to their anchor's own fraud.**

- **amlsim** — `C1` 0.975, `M3` 0.996, `S2` 1.000. The same detector scores 1.000 on amlsim's
  own labelled fraud in the same window, where sorting by amount alone already reaches 0.456. A
  near-perfect fold on an anchor like that says the generator is legible, not that anything
  generalised.
- **paysim** — `S1` 0.275. It sits above paysim's own labelled fraud (0.152) in the same window.
  An unseen family the detector finds *easier* than the ones it trains on is a statement about the
  injected rows.

## The matrix

### amlsim

**The headline fold is measured.** M3: PR-AUC 0.996 recall@1%FPR 1.000 precision@100 1.00 (150
positives)

| held out | outcome | PR-AUC | rec@FPR | p@k | positives | floor | probe | why |
| --- | --- | ---: | ---: | ---: | ---: | ---: | ---: | --- |
| `C1` Bust-out | measured | 0.975 | 1.000 | 0.79 | 80 | 0.000 | 0.236 | clears the floor (0.000); the anchor's own fraud scores 1.000 |
| `C2` UPI collect-request / APP scam | **withheld** | _(1.000)_ | _(1.000)_ | _(0.18)_ | 18 | 0.000 | 0.000 | 18 positives against a floor of 30 — recall moves 5.6% per row here, so this is reported as missing rather than as a low score |
| `C3` Instant-A2A pass-through | **withheld** | _(1.000)_ | _(1.000)_ | _(0.12)_ | 12 | 0.000 | 0.001 | 12 positives against a floor of 30 — recall moves 8.3% per row here, so this is reported as missing rather than as a low score |
| `M1` Boundary probing / paced evasion | **withheld** | _(0.997)_ | _(1.000)_ | _(0.60)_ | 60 | 0.000 | 0.164 | M1 is a `template` vector: it emits schema-valid traffic of the right shape, but its defining tell is not modelled yet — Today it paces under a *static* reporting ceiling. The vector the architecture argues for paces under the detector's own learned boundary; that trajectory is the optimiser's search, surfaced as an artefact in ticket 13. The number in this row measures the pipeline, not the family |
| `M2` Synthetic-identity lifecycle | **withheld** | _(0.507)_ | _(0.625)_ | _(0.26)_ | 48 | 0.000 | 0.013 | `sender_in_anchor` alone separates the injected M2 rows from the anchor at PR-AUC 1.000 — this fold measures provenance, not detection, and every number in it inherits that |
| `M3` First-party / friendly fraud | measured | 0.996 | 1.000 | 1.00 | 150 | 0.034 | 0.355 | clears the floor (0.034); the anchor's own fraud scores 1.000 |
| `S1` Mule network & layering | **withheld** | _(1.000)_ | _(1.000)_ | _(0.20)_ | 20 | 0.000 | 0.001 | 20 positives against a floor of 30 — recall moves 5.0% per row here, so this is reported as missing rather than as a low score |
| `S2` Card testing / BIN enumeration | measured | 1.000 | 1.000 | 0.80 | 80 | 0.063 | 0.266 | clears the floor (0.063); the anchor's own fraud scores 1.000 |
| `S3` Account takeover via drift | **withheld** | _(1.000)_ | _(1.000)_ | _(0.12)_ | 12 | 0.000 | 0.000 | 12 positives against a floor of 30 — recall moves 8.3% per row here, so this is reported as missing rather than as a low score |

_floor_ is `amount_only`: rank by amount, no model, direction chosen on the training window.
_probe_ is the provenance classifier's PR-AUC at telling the injected rows from the anchor's own.
Numbers in brackets are withheld — they exist, and nothing may be concluded from them.

Committed boundary `0400e45485d9de69`, embargo 172,800s, seed 1337, sample fraction 1.0.
The headline fold trains on 930,834 rows (1,539 fraud) and is measured on 385,515 (150 positives, base rate 0.0389%).

**Against this anchor's own fraud.** The same detector reaches PR-AUC 1.000 on amlsim's own
labelled fraud in the same test window (544 real positives, `artifacts/detector/amlsim.json`),
where sorting by amount alone reaches 0.456. A held-out family that scores *above* that line is
not a generalisation result — it is a statement about the injected rows, which is the reading
ticket 10 arrived at from the other side.

| guard | what it checked | result |
| --- | --- | ---: |
| family carve-out | the training rows and every row the detector has fitted on, replay included | 2,790,963 rows, 0 leaked |
| out-of-time embargo | committed gap 172,800s | actual gap 172,800s |
| haystack | legit rows in the test window | 385,365 of 385,365 kept |

**One contract field.** The worst single field on this anchor's `M3` rows is `log_amount`, at
PR-AUC 0.0027 against a base rate of 0.0002 — trivially separable: **False**. Anything one field
can do here, a model does first.

**A whole model.** A classifier given the fold's own features sorts the injected rows from the
anchor's own at PR-AUC **0.355** (3-fold cross-validated, base rate 0.0004, 150 injected rows in
385,515) — separable: **False**. The detector reaches 0.996 on the same rows. Every positive in
this fold is injected and every negative is real, so a probe that scores at or above the detector
means the fold cannot tell detection apart from provenance.

### paysim

**The headline fold is `withheld`.** A classifier sorts the injected M3 rows from this anchor's
own traffic at PR-AUC 0.970, against 0.893 for the detector on the same rows — every positive here
is injected and every negative is real, so the fold cannot tell detection apart from provenance.

| held out | outcome | PR-AUC | rec@FPR | p@k | positives | floor | probe | why |
| --- | --- | ---: | ---: | ---: | ---: | ---: | ---: | --- |
| `C1` Bust-out | **withheld** | _(0.604)_ | _(0.992)_ | _(0.54)_ | 120 | 0.037 | 0.906 | a classifier sorts the injected C1 rows from this anchor's own traffic at PR-AUC 0.906, against 0.604 for the detector on the same rows — every positive here is injected and every negative is real, so the fold cannot tell detection apart from provenance |
| `C2` UPI collect-request / APP scam | **withheld** | _(0.060)_ | _(1.000)_ | _(0.07)_ | 21 | 0.008 | 0.002 | 21 positives against a floor of 30 — recall moves 4.8% per row here, so this is reported as missing rather than as a low score |
| `C3` Instant-A2A pass-through | **withheld** | _(0.006)_ | _(0.600)_ | _(0.00)_ | 20 | 0.000 | 0.003 | 20 positives against a floor of 30 — recall moves 5.0% per row here, so this is reported as missing rather than as a low score |
| `M1` Boundary probing / paced evasion | **withheld** | _(0.155)_ | _(0.978)_ | _(0.09)_ | 90 | 0.000 | 0.945 | M1 is a `template` vector: it emits schema-valid traffic of the right shape, but its defining tell is not modelled yet — Today it paces under a *static* reporting ceiling. The vector the architecture argues for paces under the detector's own learned boundary; that trajectory is the optimiser's search, surfaced as an artefact in ticket 13. The number in this row measures the pipeline, not the family |
| `M2` Synthetic-identity lifecycle | **withheld** | _(0.680)_ | _(0.909)_ | _(0.55)_ | 88 | 0.085 | 0.884 | `sender_in_anchor` alone separates the injected M2 rows from the anchor at PR-AUC 1.000 — this fold measures provenance, not detection, and every number in it inherits that |
| `M3` First-party / friendly fraud | **withheld** | _(0.893)_ | _(0.967)_ | _(1.00)_ | 300 | 0.003 | 0.970 | a classifier sorts the injected M3 rows from this anchor's own traffic at PR-AUC 0.970, against 0.893 for the detector on the same rows — every positive here is injected and every negative is real, so the fold cannot tell detection apart from provenance |
| `S1` Mule network & layering | measured | 0.275 | 0.860 | 0.15 | 50 | 0.001 | 0.298 | clears the floor (0.001), but **beats this anchor's own fraud (0.152)** — easier unseen than seen |
| `S2` Card testing / BIN enumeration | **withheld** | _(0.005)_ | _(0.125)_ | _(0.00)_ | 120 | 0.017 | 1.000 | a classifier sorts the injected S2 rows from this anchor's own traffic at PR-AUC 1.000, against 0.005 for the detector on the same rows — every positive here is injected and every negative is real, so the fold cannot tell detection apart from provenance |
| `S3` Account takeover via drift | **withheld** | _(0.916)_ | _(0.958)_ | _(0.22)_ | 24 | 0.095 | 0.042 | 24 positives against a floor of 30 — recall moves 4.2% per row here, so this is reported as missing rather than as a low score |

_floor_ is `amount_only`: rank by amount, no model, direction chosen on the training window.
_probe_ is the provenance classifier's PR-AUC at telling the injected rows from the anchor's own.
Numbers in brackets are withheld — they exist, and nothing may be concluded from them.

Committed boundary `6ea0b94c1c8d3d3a`, embargo 90,000s, seed 1337, sample fraction 0.1.
The headline fold trains on 446,368 rows (523 fraud) and is measured on 150,656 (300 positives, base rate 0.1991%).

**Against this anchor's own fraud.** The same detector reaches PR-AUC 0.152 on paysim's own
labelled fraud in the same test window (410 real positives, `artifacts/detector/paysim.json`),
where sorting by amount alone reaches 0.057. A held-out family that scores *above* that line is
not a generalisation result — it is a statement about the injected rows, which is the reading
ticket 10 arrived at from the other side.

| guard | what it checked | result |
| --- | --- | ---: |
| family carve-out | the training rows and every row the detector has fitted on, replay included | 1,338,581 rows, 0 leaked |
| out-of-time embargo | committed gap 90,000s | actual gap 90,000s |
| haystack | legit rows in the test window | 150,356 of 150,356 kept |

**One contract field.** The worst single field on this anchor's `M3` rows is `hour_of_day`, at
PR-AUC 0.0031 against a base rate of 0.0005 — trivially separable: **False**. Anything one field
can do here, a model does first.

**A whole model.** A classifier given the fold's own features sorts the injected rows from the
anchor's own at PR-AUC **0.970** (3-fold cross-validated, base rate 0.0020, 300 injected rows in
150,656) — separable: **True**. The detector reaches 0.893 on the same rows. Every positive in
this fold is injected and every negative is real, so a probe that scores at or above the detector
means the fold cannot tell detection apart from provenance.

## Folds that carry no number

- **amlsim / `C2`** — withheld: 18 positives against a floor of 30 — recall moves 5.6% per row
  here, so this is reported as missing rather than as a low score.
- **amlsim / `C3`** — withheld: 12 positives against a floor of 30 — recall moves 8.3% per row
  here, so this is reported as missing rather than as a low score.
- **amlsim / `M1`** — withheld: M1 is a `template` vector: it emits schema-valid traffic of the
  right shape, but its defining tell is not modelled yet — Today it paces under a *static* reporting
  ceiling. The vector the architecture argues for paces under the detector's own learned boundary;
  that trajectory is the optimiser's search, surfaced as an artefact in ticket 13. The number in
  this row measures the pipeline, not the family.
- **amlsim / `M2`** — withheld: `sender_in_anchor` alone separates the injected M2 rows from the
  anchor at PR-AUC 1.000 — this fold measures provenance, not detection, and every number in it
  inherits that.
- **amlsim / `S1`** — withheld: 20 positives against a floor of 30 — recall moves 5.0% per row
  here, so this is reported as missing rather than as a low score.
- **amlsim / `S3`** — withheld: 12 positives against a floor of 30 — recall moves 8.3% per row
  here, so this is reported as missing rather than as a low score.
- **paysim / `C1`** — withheld: a classifier sorts the injected C1 rows from this anchor's own
  traffic at PR-AUC 0.906, against 0.604 for the detector on the same rows — every positive here is
  injected and every negative is real, so the fold cannot tell detection apart from provenance.
- **paysim / `C2`** — withheld: 21 positives against a floor of 30 — recall moves 4.8% per row
  here, so this is reported as missing rather than as a low score.
- **paysim / `C3`** — withheld: 20 positives against a floor of 30 — recall moves 5.0% per row
  here, so this is reported as missing rather than as a low score.
- **paysim / `M1`** — withheld: M1 is a `template` vector: it emits schema-valid traffic of the
  right shape, but its defining tell is not modelled yet — Today it paces under a *static* reporting
  ceiling. The vector the architecture argues for paces under the detector's own learned boundary;
  that trajectory is the optimiser's search, surfaced as an artefact in ticket 13. The number in
  this row measures the pipeline, not the family.
- **paysim / `M2`** — withheld: `sender_in_anchor` alone separates the injected M2 rows from the
  anchor at PR-AUC 1.000 — this fold measures provenance, not detection, and every number in it
  inherits that.
- **paysim / `M3`** — withheld: a classifier sorts the injected M3 rows from this anchor's own
  traffic at PR-AUC 0.970, against 0.893 for the detector on the same rows — every positive here is
  injected and every negative is real, so the fold cannot tell detection apart from provenance.
- **paysim / `S2`** — withheld: a classifier sorts the injected S2 rows from this anchor's own
  traffic at PR-AUC 1.000, against 0.005 for the detector on the same rows — every positive here is
  injected and every negative is real, so the fold cannot tell detection apart from provenance.
- **paysim / `S3`** — withheld: 24 positives against a floor of 30 — recall moves 4.2% per row
  here, so this is reported as missing rather than as a low score.

**Not measured.** `amlworld` is not downloaded, so it has no matrix here. `banksim` is not downloaded, so it has no matrix here.

## Where the numbers came from

```bash
make loao        # or: python scripts/build_loao.py
```
