# The sequence model, and whether it earned its seat

_Generated by `scripts/build_sequence.py` from the anchors on disk. Every number below traces to
`artifacts/sequence/<anchor>.json`, which carries the split digest, the seed, the model cards, the
compute cost and the three audits that decide whether a fold means anything. Do not edit this file
— re-run `make sequence`._

Ticket 17. A GRU over per-entity history is measured against the tuned LightGBM baseline on the
same out-of-time split at the same operating point, on the drift arc — S3 account takeover and C1
bust-out — with the sudden and gradual ends of that arc reported separately. The model enters a
reported table only if it beats the baseline on the gradual end. The comparison is published
either way.

## The result

**The sequence model did not earn its seat on any anchor measured, and that is the result.** It is
not in the headline table, `config/defend/sequence.yaml` stays `enabled: false`, and the
hand-rolled per-row feature table is what ships. The comparison is published because a negative
result that lives on one laptop is not a result, and because the next person to reach for a GRU on
this arc should be able to read what happened when we did.

| anchor | family | history per entity | sequence | lgbm | amount | fold |
| --- | --- | ---: | ---: | ---: | ---: | --- |
| amlsim | S3 | 28.5 steps | (0.391) | (0.997) | (0.013) | withheld |
| amlsim | C1 | 28.5 steps | (0.300) | (0.998) | (0.000) | withheld |
| paysim | S3 | 1.0 steps | (0.933) | (0.947) | (0.122) | withheld |
| paysim | C1 | 1.0 steps | (0.987) | (0.773) | (0.064) | withheld |

Every number above is the **gradual** arc — the end the gate is decided on. All of them are in
brackets, because no fold here can carry a claim; the per-fold sections say which audit refused
each one. `history per entity` is how many steps of its own account's past a typical *anchor* row
carries, which is the precondition this whole model rests on.

**The answer flips exactly along that history column, and it is a refusal at both ends.** Where
the anchor's accounts have real pasts the sequence model loses on merit — amlsim/S3 0.391 against
0.997; amlsim/C1 0.300 against 0.998 — so the trajectory it reads is worth less than the per-row
table it was supposed to improve on. Where the anchor has no per-entity history at all, every
sender appearing once so that a real window is one step long, it is level or ahead — paysim/S3
0.933 against 0.947; paysim/C1 0.987 against 0.773 — and that is the result that disqualifies it
rather than the one that promotes it: the only history in those folds belongs to the injected
episodes, so a model that reads history is reading which generator wrote the row before it reads
any behaviour. The `history` audit is what catches it, and it exists because this is the model it
had to be built for.

**Read the audits before the tables.** Every positive in these folds is an injected synthetic row
and every negative is a real one, so a fold where one contract field — or, for this model
specifically, one window length — separates the two is measuring which generator wrote the row
rather than whether anything detects fraud.

## amlsim

Committed split digest `0400e45485d9de69`, seed 1337, operating point 1% FPR and k=100 from
`config/eval/leave_one_attack_out.yaml`. The champion is LGBMDetector, not the ensemble the loop
ships — the ticket's bar is the supervised baseline, and a blend would move two things at once.

### S3 — Account takeover via drift

Two batches of this family, identical but for `ramp`: **0.0** for the sudden end and **1.0** for
the gradual one, both read out of S3's own declared search space so neither leaves the realism
envelope. The family is held out of training in both cases, so what is measured is generalisation
to an unseen family and not recall on a seen one.

**Withheld.** A classifier sorts the injected S3 rows from this anchor's own traffic at PR-AUC
0.974 — every positive in this fold is injected and every negative is real, so it cannot tell
detection apart from provenance for either system.

The numbers below exist and are printed in brackets. They are evidence about the pipeline; they
are not evidence about this family, and nothing in this repo quotes them.

- **Fold** — 385,821 holdout rows carrying 456 S3 positives (216 sudden, 240 gradual), against 930,948 training rows with 1,653 fraud and not one S3 row.
- **History** — the anchor's own rows carry 28.5 steps of per-src history on average (1% of them have none at all); the injected episodes carry 30.8. Window length alone separates the two at PR-AUC 0.001: **ok**.
- **Commensurability** — worst single contract field `log_amount` at PR-AUC 0.0081: **ok**.
- **Provenance** — a classifier over the detector's own features sorts injected rows from real ones at PR-AUC 0.974 against a base rate of 0.0012: **separable**.

| system | arc | PR-AUC | rec@FPR | p@k | positives | one-threshold recall |
| --- | --- | ---: | ---: | ---: | ---: | ---: |
| sequence | sudden | (0.376) | (1.000) | (0.290) | 216 | 1.000 |
| sequence | gradual | (0.391) | (1.000) | (0.290) | 240 | 1.000 |
| lgbm | sudden | (1.000) | (1.000) | (1.000) | 216 | 1.000 |
| lgbm | gradual | (0.997) | (1.000) | (1.000) | 240 | 1.000 |
| amount_only | sudden | (0.014) | (0.019) | (0.030) | 216 | 0.019 |
| amount_only | gradual | (0.013) | (0.017) | (0.030) | 240 | 0.017 |

A number in brackets is one this file will not quote — that arc is too thin to carry a claim, or
the whole fold is. Each arc is ranked against *every* legit row of the fold, so only the needles
change between the two rows and the base rate does not. `one-threshold recall` is measured from
the whole fold's legit traffic rather than from the arc's own selection, and it matches `rec@FPR`
in every row above: that agreement is what says the two arcs were read at one operating point
rather than at two.

| system | whole fold PR-AUC | evasion | friction | compute |
| --- | ---: | ---: | ---: | --- |
| sequence | 0.549 | 0.2% | 3.2% | 21s fit, 4s score, 86,095 rows/s, 25,794 params |
| lgbm | 0.999 | 0.0% | 3.1% | 32s fit, 10s score, 37,678 rows/s |
| amount_only | 0.014 | — | — | — |

Compute is in the table because the trade has to be visible: a lift is only readable next to what
it cost. `evasion` is the share of the held-out family the policy let through untouched and
`friction` the share of legit traffic that carried any action; the floor row has neither because
it has no policy.

**Gate.** Never reached. It is decided on the gradual arc and the fold was refused before the
comparison could carry anything, so the bracketed gradual row above is what the gate *would* have
read and not what it did.

### C1 — Bust-out

Two batches of this family, identical but for `ramp`: **0.0** for the sudden end and **0.6** for
the gradual one, both read out of C1's own declared search space so neither leaves the realism
envelope. The family is held out of training in both cases, so what is measured is generalisation
to an unseen family and not recall on a seen one.

**Withheld.** A classifier sorts the injected C1 rows from this anchor's own traffic at PR-AUC
0.688 — every positive in this fold is injected and every negative is real, so it cannot tell
detection apart from provenance for either system.

The numbers below exist and are printed in brackets. They are evidence about the pipeline; they
are not evidence about this family, and nothing in this repo quotes them.

- **Fold** — 385,915 holdout rows carrying 550 C1 positives (290 sudden, 260 gradual), against 930,843 training rows with 1,548 fraud and not one C1 row.
- **History** — the anchor's own rows carry 28.5 steps of per-src history on average (1% of them have none at all); the injected episodes carry 32.0. Window length alone separates the two at PR-AUC 0.001: **ok**.
- **Commensurability** — worst single contract field `log_amount` at PR-AUC 0.0061: **ok**.
- **Provenance** — a classifier over the detector's own features sorts injected rows from real ones at PR-AUC 0.688 against a base rate of 0.0014: **separable**.

| system | arc | PR-AUC | rec@FPR | p@k | positives | one-threshold recall |
| --- | --- | ---: | ---: | ---: | ---: | ---: |
| sequence | sudden | (0.354) | (1.000) | (0.300) | 290 | 1.000 |
| sequence | gradual | (0.300) | (1.000) | (0.180) | 260 | 1.000 |
| lgbm | sudden | (1.000) | (1.000) | (1.000) | 290 | 1.000 |
| lgbm | gradual | (0.998) | (1.000) | (1.000) | 260 | 1.000 |
| amount_only | sudden | (0.011) | (0.010) | (0.030) | 290 | 0.010 |
| amount_only | gradual | (0.000) | (0.004) | (0.000) | 260 | 0.004 |

A number in brackets is one this file will not quote — that arc is too thin to carry a claim, or
the whole fold is. Each arc is ranked against *every* legit row of the fold, so only the needles
change between the two rows and the base rate does not. `one-threshold recall` is measured from
the whole fold's legit traffic rather than from the arc's own selection, and it matches `rec@FPR`
in every row above: that agreement is what says the two arcs were read at one operating point
rather than at two.

| system | whole fold PR-AUC | evasion | friction | compute |
| --- | ---: | ---: | ---: | --- |
| sequence | 0.487 | 0.0% | 3.2% | 20s fit, 4s score, 88,978 rows/s, 25,794 params |
| lgbm | 0.999 | 0.2% | 3.1% | 32s fit, 10s score, 38,799 rows/s |
| amount_only | 0.006 | — | — | — |

Compute is in the table because the trade has to be visible: a lift is only readable next to what
it cost. `evasion` is the share of the held-out family the policy let through untouched and
`friction` the share of legit traffic that carried any action; the floor row has neither because
it has no policy.

**Gate.** Never reached. It is decided on the gradual arc and the fold was refused before the
comparison could carry anything, so the bracketed gradual row above is what the gate *would* have
read and not what it did.

## paysim

Committed split digest `6ea0b94c1c8d3d3a`, seed 1337, operating point 1% FPR and k=100 from
`config/eval/leave_one_attack_out.yaml`. The champion is LGBMDetector, not the ensemble the loop
ships — the ticket's bar is the supervised baseline, and a blend would move two things at once.

### S3 — Account takeover via drift

Two batches of this family, identical but for `ramp`: **0.0** for the sudden end and **1.0** for
the gradual one, both read out of S3's own declared search space so neither leaves the realism
envelope. The family is held out of training in both cases, so what is measured is generalisation
to an unseen family and not recall on a seen one.

**Withheld.** Window length alone sorts the injected S3 rows from this anchor's own traffic at
PR-AUC 0.933 — the anchor's own rows carry 1.0 steps of history on average against 9.1 for the
injected ones, so a model that reads per-entity history reads provenance here before it reads
behaviour.

The numbers below exist and are printed in brackets. They are evidence about the pipeline; they
are not evidence about this family, and nothing in this repo quotes them.

- **Fold** — 151,244 holdout rows carrying 888 S3 positives (384 sudden, 504 gradual), against 446,478 training rows with 633 fraud and not one S3 row.
- **History** — the anchor's own rows carry 1.0 steps of per-src history on average (100% of them have none at all); the injected episodes carry 9.1. Window length alone separates the two at PR-AUC 0.933: **separable**.
- **Commensurability** — worst single contract field `log_amount` at PR-AUC 0.0918: **ok**.
- **Provenance** — a classifier over the detector's own features sorts injected rows from real ones at PR-AUC 0.955 against a base rate of 0.0059: **separable**.

| system | arc | PR-AUC | rec@FPR | p@k | positives | one-threshold recall |
| --- | --- | ---: | ---: | ---: | ---: | ---: |
| sequence | sudden | (0.958) | (0.997) | (0.950) | 384 | 0.997 |
| sequence | gradual | (0.933) | (0.972) | (0.970) | 504 | 0.972 |
| lgbm | sudden | (0.953) | (0.979) | (1.000) | 384 | 0.979 |
| lgbm | gradual | (0.947) | (0.958) | (1.000) | 504 | 0.958 |
| amount_only | sudden | (0.171) | (0.323) | (0.520) | 384 | 0.323 |
| amount_only | gradual | (0.122) | (0.232) | (0.460) | 504 | 0.232 |

A number in brackets is one this file will not quote — that arc is too thin to carry a claim, or
the whole fold is. Each arc is ranked against *every* legit row of the fold, so only the needles
change between the two rows and the base rate does not. `one-threshold recall` is measured from
the whole fold's legit traffic rather than from the arc's own selection, and it matches `rec@FPR`
in every row above: that agreement is what says the two arcs were read at one operating point
rather than at two.

| system | whole fold PR-AUC | evasion | friction | compute |
| --- | ---: | ---: | ---: | --- |
| sequence | 0.962 | 2.0% | 6.1% | 9s fit, 2s score, 85,548 rows/s, 25,794 params |
| lgbm | 0.956 | 3.2% | 2.6% | 15s fit, 4s score, 33,866 rows/s |
| amount_only | 0.179 | — | — | — |

Compute is in the table because the trade has to be visible: a lift is only readable next to what
it cost. `evasion` is the share of the held-out family the policy let through untouched and
`friction` the share of legit traffic that carried any action; the floor row has neither because
it has no policy.

**Gate.** Never reached. It is decided on the gradual arc and the fold was refused before the
comparison could carry anything, so the bracketed gradual row above is what the gate *would* have
read and not what it did.

### C1 — Bust-out

Two batches of this family, identical but for `ramp`: **0.0** for the sudden end and **0.6** for
the gradual one, both read out of C1's own declared search space so neither leaves the realism
envelope. The family is held out of training in both cases, so what is measured is generalisation
to an unseen family and not recall on a seen one.

**Withheld.** Window length alone sorts the injected C1 rows from this anchor's own traffic at
PR-AUC 0.985 — the anchor's own rows carry 1.0 steps of history on average against 8.2 for the
injected ones, so a model that reads per-entity history reads provenance here before it reads
behaviour.

The numbers below exist and are printed in brackets. They are evidence about the pipeline; they
are not evidence about this family, and nothing in this repo quotes them.

- **Fold** — 151,536 holdout rows carrying 1,180 C1 positives (590 sudden, 590 gradual), against 446,354 training rows with 509 fraud and not one C1 row.
- **History** — the anchor's own rows carry 1.0 steps of per-src history on average (100% of them have none at all); the injected episodes carry 8.2. Window length alone separates the two at PR-AUC 0.985: **separable**.
- **Commensurability** — worst single contract field `log_amount` at PR-AUC 0.0214: **ok**.
- **Provenance** — a classifier over the detector's own features sorts injected rows from real ones at PR-AUC 0.956 against a base rate of 0.0078: **separable**.

| system | arc | PR-AUC | rec@FPR | p@k | positives | one-threshold recall |
| --- | --- | ---: | ---: | ---: | ---: | ---: |
| sequence | sudden | (0.987) | (0.997) | (0.990) | 590 | 0.997 |
| sequence | gradual | (0.987) | (0.998) | (1.000) | 590 | 0.998 |
| lgbm | sudden | (0.811) | (0.997) | (1.000) | 590 | 0.997 |
| lgbm | gradual | (0.773) | (1.000) | (1.000) | 590 | 1.000 |
| amount_only | sudden | (0.067) | (0.190) | (0.310) | 590 | 0.190 |
| amount_only | gradual | (0.064) | (0.166) | (0.300) | 590 | 0.166 |

A number in brackets is one this file will not quote — that arc is too thin to carry a claim, or
the whole fold is. Each arc is ranked against *every* legit row of the fold, so only the needles
change between the two rows and the base rate does not. `one-threshold recall` is measured from
the whole fold's legit traffic rather than from the arc's own selection, and it matches `rec@FPR`
in every row above: that agreement is what says the two arcs were read at one operating point
rather than at two.

| system | whole fold PR-AUC | evasion | friction | compute |
| --- | ---: | ---: | ---: | --- |
| sequence | 0.992 | 0.4% | 7.4% | 7s fit, 2s score, 89,041 rows/s, 25,794 params |
| lgbm | 0.873 | 0.3% | 4.2% | 16s fit, 7s score, 21,366 rows/s |
| amount_only | 0.097 | — | — | — |

Compute is in the table because the trade has to be visible: a lift is only readable next to what
it cost. `evasion` is the share of the held-out family the policy let through untouched and
`friction` the share of legit traffic that carried any action; the floor row has neither because
it has no policy.

**Gate.** Never reached. It is decided on the gradual arc and the fold was refused before the
comparison could carry anything, so the bracketed gradual row above is what the gate *would* have
read and not what it did.

## What this says about the leave-one-attack-out matrix

**amlsim / C1.** `artifacts/loao/amlsim.json` reports this family as *measured*, on a probe that saw 80 injected rows and scored PR-AUC 0.236. The same probe here sees 550 and scores 0.688, over the bar. Nothing about the generator changed between the two runs — the episode count did. Read the matrix row as underpowered rather than as a contradiction, and `make loao` at this episode count would be
the way to settle it.

## Anchors not measured

amlworld, banksim are not on disk, so they are named as missing rather than reported as a zero.
See the data card for how to fetch them.

## What this does not settle

One architecture, one seed per anchor, one set of hyperparameters read from
`config/defend/sequence.yaml`. A negative result here is evidence that *this* sequence model, on
*these* anchors, does not beat the per-row table on the arc it was built for — not that no
sequence model could. What would change the answer is an anchor with real per-entity histories on
both sides of the label, which is the precondition the `history` audit measures and the one PaySim
does not meet.
