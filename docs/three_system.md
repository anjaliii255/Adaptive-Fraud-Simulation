# The three-system table

_Generated by `scripts/build_three_system.py` from the anchors on disk. Every number below
traces to `artifacts/three_system/<anchor>.json`, which carries the eval config, the committed
split digest and every seed that produced it. Do not edit this file — re-run `make table`._

Three systems, one holdout, one operating point:

| | system | trained on | what it stands for |
| --- | --- | --- | --- |
| **A** | `A_baseline` | the anchor's real rows and real labels | what a team has today |
| **B** | `B_smote` | the same rows plus row-level oversampling | what a team tries first |
| **C** | `C_adaptive` | the same rows plus the attacker–defender loop | the claim |

**System B is here to make System C falsifiable.** Row-level oversampling can move an amount and
a timestamp; it cannot invent a new fan-in shape, a new pacing strategy, or a beneficiary that
never existed — which is precisely the gap the adaptive system claims to fill. If C does not beat
B on the held-out column, the project reduces to an expensive way of duplicating rows, and this
is the table where that shows up. **It is built to be able to say so.**

**Operating point: recall at 1% FPR, precision@100**, fixed
in `config/eval/leave_one_attack_out.yaml` before any of these numbers existed, and applied
identically to all three rows — including where the action bands sit, because `evasion_rate` in
the same table is a function of them.

## Two columns, because one is not a result

- **unseen** — the held-out family (`M3`), which no system trained on. This
  is the claim. It is the same carve-out `make loao` builds: same pool, same committed boundary,
  same three guards, same commensurability audit and provenance probe.
- **known** — the fraud every system *did* train on: the anchor's own labelled rows, scored on
  the same window against the same legit haystack. This is the price of the claim. A system that
  buys the unseen column by giving up the known one has traded rather than improved.

**These are not `docs/loao.md`'s numbers, and they are not meant to be.** The fold is the same
one — same pool, same boundary, same guards — but the *training set* is the whole point of this
table: System A sees the anchor's real rows alone, where the matrix's detector trains on the
entire training side, injected families included. A row here and a row there are answers to
different questions and do not belong in one comparison.

The two columns share their negatives, asserted rather than assumed
(`three_system.assert_same_haystack`): recall at a fixed FPR is a quantile of the negatives, so
two haystacks would be two operating points wearing one table. Families the pool carries but
nobody trained on appear in neither column — they are neither the claim nor the control, and
counting them as negatives would label real fraud as legit traffic.

## How a difference becomes a result

Every cell is a mean over seeds with its seed-to-seed spread beside it, and every comparison is
**paired by seed**: both systems see the same fold, the same pool and the same fitted procedure
on a given seed, so the difference between them is the only thing the seed does not also move.

The seed moves the whole pipeline, not just the fit — the attack episodes that go into the pool,
the SMOTE draw, the optimiser's search and the model's own randomness all turn with it. That is
deliberate: the question a reader has is whether the *system* is better, and a spread measured
over refits alone would answer a narrower one.

A gap smaller than its own spread is reported as **inside the noise**, whichever way it points.
The sign test counts which way each seed fell; over
3 seed(s) it cannot reach p < 0.05 by construction, and that
is stated rather than worked around.

**The unseen column inherits ticket 11's verdicts.** Where that harness withholds a fold — too
few positives, separable from the anchor by one contract field, separable by a whole model, or a
`template` vector whose defining tell is not modelled yet — the numbers move out of `metrics`
into `withheld_metrics` and are printed **in brackets** next to the reason. 2 of 6
system-columns across 2 anchor(s) carry a quotable unseen number.

## The verdict

- **amlsim** — C_adaptive beats B_smote on the unseen column: recall_at_fixed_fpr +0.7573 ±
  0.0442 over 3 seeds, 3/3 in that direction, sign-test p = 0.125. The column itself is withheld:
  a classifier sorts the injected M3 rows from this anchor's own traffic at PR-AUC 0.998, against
  1.000 for the detector on the same rows — every positive here is injected and every negative is
  real, so the fold cannot tell detection apart from provenance.
- **paysim** — C_adaptive beats B_smote on the unseen column: recall_at_fixed_fpr +0.5678 ±
  0.0356 over 3 seeds, 3/3 in that direction, sign-test p = 0.125. The column itself is withheld:
  a classifier sorts the injected M3 rows from this anchor's own traffic at PR-AUC 0.970, against
  0.285 for the detector on the same rows — every positive here is injected and every negative is
  real, so the fold cannot tell detection apart from provenance.

**Not measured.** `amlworld` is not downloaded, so it has no table here. `banksim` is not
downloaded, so it has no table here.

## Per anchor

### amlsim

**C_adaptive beats B_smote on the unseen column: recall_at_fixed_fpr +0.7573 ± 0.0442 over 3
seeds, 3/3 in that direction, sign-test p = 0.125. The column itself is withheld: a classifier
sorts the injected M3 rows from this anchor's own traffic at PR-AUC 0.998, against 1.000 for the
detector on the same rows — every positive here is injected and every negative is real, so the
fold cannot tell detection apart from provenance.**

On PR-AUC in the same column: C_adaptive beats B_smote on the unseen column: pr_auc +0.8775 ±
0.0291 over 3 seeds, 3/3 in that direction, sign-test p = 0.125. The column itself is withheld: a
classifier sorts the injected M3 rows from this anchor's own traffic at PR-AUC 0.998, against
1.000 for the detector on the same rows — every positive here is injected and every negative is
real, so the fold cannot tell detection apart from provenance.

| system | known PR-AUC | known recall@1%FPR | unseen PR-AUC (M3) | unseen recall@1%FPR | unseen P@100 | train rows | train fraud | generated |
| --- | --- | --- | --- | --- | --- | --- | --- | --- |
| A_baseline | 1.000 ± 0.000 | 1.000 ± 0.000 | 0.105 ± 0.007 | 0.214 ± 0.035 | 0.17 ± 0.05 | 930,465 | 1,170 | 0 |
| B_smote | 0.996 ± 0.002 | 0.996 ± 0.002 | 0.121 ± 0.032 | 0.243 ± 0.044 | 0.22 ± 0.06 | 931,635 | 2,340 | 1,170 |
| C_adaptive | 0.994 ± 0.004 | 0.994 ± 0.003 | [0.998 ± 0.003] | [1.000 ± 0.000] | [1.00 ± 0.00] | 935,469 | 6,174 | 5,004 |
| amount floor | 0.456 ± 0.000 | 0.474 ± 0.000 | 0.030 ± 0.006 | 0.034 ± 0.006 | 0.04 ± 0.01 | — | — | — |

_mean ± sd over 3 seed(s) [1337, 7, 21]; **[brackets]** mark numbers the harness withholds; detector backend: lightgbm 4.5.0_

**The fold.** All three systems train on 930,465 rows (1,170 fraud) out of a 1,324,044-row pool,
and are scored on one test window: **unseen** 385,515 rows / 150 positives (base rate 0.0389%, all
injected), **known** 385,909 rows / 544 positives (544 of them the anchor's own labelled fraud).
Committed boundary `0400e45485d9de69`, embargo 172,800s, sample fraction 1.0.

| guard | what it checked | result |
| --- | --- | ---: |
| family carve-out | the training rows | 930,834 rows, 0 leaked |
| out-of-time embargo | committed gap 172,800s | actual gap 172,800s |
| haystack | legit rows in the test window | 385,365 of 385,365 kept |
| one haystack, two columns | legit rows shared by both columns | 385,365 shared |

**One contract field.** The worst single field on this anchor's `M3` rows is `log_amount`, at
PR-AUC 0.0027 against a base rate of 0.0002 — trivially separable: **False**.

**The provenance probe.** A classifier given the fold's own features sorts the injected `M3` rows
from this anchor's own traffic at PR-AUC **0.355** (3-fold cross-validated, 150 injected rows in
385,515) — separable: **False**. Every positive in the unseen column is injected and every
negative is real, so a probe at or above the detector's own score means that column cannot tell
detection apart from provenance.

**Against this anchor's own fraud.** The committed baseline reaches PR-AUC 1.000 on amlsim's own
labelled fraud in the same test window (544 real positives, `artifacts/detector/amlsim.json`),
where sorting by amount alone reaches 0.456. Any row above that line on the unseen column is
easier unseen than seen, which is a statement about the injected rows rather than a generalisation
result.

**System C's loop.** 12 rounds of 12 episodes, searching `S1`, `S2`, `S3` with the `search`
allocation. Every candidate batch is audit-gated on the `envelope` rule before the detector is
allowed to train on it, and a rejected batch is discarded rather than learned from — see
`afl/attack/multi.py:AUDIT_RULES` for what the two rules disagree about.

| seed | rounds | rejected | would reject: envelope / lift | rows kept | evasion first → last | min / max |
| --- | ---: | ---: | ---: | ---: | --- | --- |
| 1337 | 12 | 0 | 0 / 11 | 4,868 | 0.876 → 0.002 | 0.000 / 0.876 |
| 7 | 12 | 0 | 0 / 12 | 4,928 | 0.872 → 0.060 | 0.000 / 0.872 |
| 21 | 12 | 0 | 0 / 12 | 5,216 | 0.863 → 0.034 | 0.000 / 0.863 |

**Does provenance explain System C?** A model given the same training rows as System C and told
only *which rows the generator wrote* — never which rows are fraud, and never a row of the
held-out family — scores PR-AUC **0.995** on the held-out column against System C's **0.998**
(4,868 generated rows to learn from, separable: **True**). On this anchor provenance alone matches
or beats System C, so System C's held-out number is the generator's fingerprint transferring
between families rather than a detector generalising to an unseen attack.

**Every comparison, paired by seed.**

| comparison | column | metric | per-seed Δ | mean ± sd | wins | sign-test p | reading |
| --- | --- | --- | --- | --- | ---: | ---: | --- |
| C_adaptive − B_smote | known | pr_auc | +0.002, -0.007, -0.002 | -0.0024 ± 0.0045 | 1/3 | 0.875 | inside the spread |
| C_adaptive − A_baseline | known | pr_auc | -0.002, -0.009, -0.007 | -0.0060 ± 0.0037 | 0/3 | 1.000 | loses |
| B_smote − A_baseline | known | pr_auc | -0.004, -0.002, -0.005 | -0.0037 ± 0.0018 | 0/3 | 1.000 | loses |
| C_adaptive − B_smote | known | recall_at_fixed_fpr | +0.002, -0.006, -0.002 | -0.0018 ± 0.0037 | 1/3 | 0.875 | inside the spread |
| C_adaptive − A_baseline | known | recall_at_fixed_fpr | -0.002, -0.007, -0.007 | -0.0055 ± 0.0032 | 0/3 | 1.000 | loses |
| B_smote − A_baseline | known | recall_at_fixed_fpr | -0.004, -0.002, -0.006 | -0.0037 ± 0.0018 | 0/3 | 1.000 | loses |
| C_adaptive − B_smote | unseen | pr_auc | +0.854, +0.868, +0.910 | +0.8775 ± 0.0291 | 3/3 | 0.125 | beats, column withheld |
| C_adaptive − A_baseline | unseen | pr_auc | +0.887, +0.900, +0.892 | +0.8929 ± 0.0067 | 3/3 | 0.125 | beats, column withheld |
| B_smote − A_baseline | unseen | pr_auc | +0.032, +0.032, -0.018 | +0.0154 ± 0.0291 | 2/3 | 0.500 | inside the spread |
| C_adaptive − B_smote | unseen | recall_at_fixed_fpr | +0.727, +0.808, +0.737 | +0.7573 ± 0.0442 | 3/3 | 0.125 | beats, column withheld |
| C_adaptive − A_baseline | unseen | recall_at_fixed_fpr | +0.780, +0.824, +0.754 | +0.7861 ± 0.0353 | 3/3 | 0.125 | beats, column withheld |
| B_smote − A_baseline | unseen | recall_at_fixed_fpr | +0.053, +0.016, +0.017 | +0.0288 ± 0.0212 | 3/3 | 0.125 | beats |

**Why it landed there.**

- **The held-out column cannot carry a claim on this anchor, so neither system's number on it is
  quotable and the difference between them is not either — a classifier sorts the injected M3 rows
  from this anchor's own traffic at PR-AUC 0.998, against 1.000 for the detector on the same rows —
  every positive here is injected and every negative is real, so the fold cannot tell detection
  apart from provenance.** Evidence: `{"outcome": "withheld", "cells_affected": "3/9"}`.
- **A model that knows nothing except which rows the generator wrote reaches System C's held-out
  score on the same rows — so C's number on that column is the generator's fingerprint transferring
  between families, not a detector generalising to an unseen attack. Both models trained on the same
  injected rows; only one of them was told what fraud is.** Evidence: `{"system_c_pr_auc": 1.0,
  "provenance_only_pr_auc": 0.997973, "trained_on": "the loop's own output, which is System C's
  training set"}`.
- **Every system is at the ceiling on the known column — this anchor's own fraud is separable by
  almost anything, so holding that column steady is not evidence of much, and neither is losing a
  little of it.** Evidence: `{"known_column_range": [0.9945, 1.0]}`.
- **System C lost ground on the attacks it already knew about — the generated rows moved the fit
  away from the families the anchor actually carries. Whatever the held-out column says, this is the
  cost side of it.** Evidence: `{"known_delta_C_minus_A": -0.005515, "sd": 0.003184, "seeds": 3}`.

**Every seed.**

| seed | system | unseen PR-AUC | unseen rec@FPR | known PR-AUC | known rec@FPR | train rows | train fraud | generated |
| ---: | --- | ---: | ---: | ---: | ---: | ---: | ---: | ---: |
| 1337 | A_baseline | 0.113 | 0.220 | 1.000 | 1.000 | 930,465 | 1,170 | 0 |
| 1337 | B_smote | 0.146 | 0.273 | 0.996 | 0.996 | 931,635 | 2,340 | 1,170 |
| 1337 | C_adaptive | 1.000 | 1.000 | 0.998 | 0.998 | 935,333 | 6,038 | 4,868 |
| 7 | A_baseline | 0.100 | 0.176 | 1.000 | 1.000 | 930,465 | 1,170 | 0 |
| 7 | B_smote | 0.132 | 0.192 | 0.998 | 0.998 | 931,635 | 2,340 | 1,170 |
| 7 | C_adaptive | 1.000 | 1.000 | 0.991 | 0.993 | 935,393 | 6,098 | 4,928 |
| 21 | A_baseline | 0.103 | 0.246 | 1.000 | 1.000 | 930,465 | 1,170 | 0 |
| 21 | B_smote | 0.084 | 0.263 | 0.995 | 0.994 | 931,635 | 2,340 | 1,170 |
| 21 | C_adaptive | 0.995 | 1.000 | 0.993 | 0.993 | 935,681 | 6,386 | 5,216 |

_Runtime: seed 1337 910s, seed 7 890s, seed 21 807s._

### paysim

**C_adaptive beats B_smote on the unseen column: recall_at_fixed_fpr +0.5678 ± 0.0356 over 3
seeds, 3/3 in that direction, sign-test p = 0.125. The column itself is withheld: a classifier
sorts the injected M3 rows from this anchor's own traffic at PR-AUC 0.970, against 0.285 for the
detector on the same rows — every positive here is injected and every negative is real, so the
fold cannot tell detection apart from provenance.**

On PR-AUC in the same column: C_adaptive beats B_smote on the unseen column: pr_auc +0.4019 ±
0.0597 over 3 seeds, 3/3 in that direction, sign-test p = 0.125. The column itself is withheld: a
classifier sorts the injected M3 rows from this anchor's own traffic at PR-AUC 0.970, against
0.285 for the detector on the same rows — every positive here is injected and every negative is
real, so the fold cannot tell detection apart from provenance.

| system | known PR-AUC | known recall@1%FPR | unseen PR-AUC (M3) | unseen recall@1%FPR | unseen P@100 | train rows | train fraud | generated |
| --- | --- | --- | --- | --- | --- | --- | --- | --- |
| A_baseline | 0.162 ± 0.016 | 0.437 ± 0.006 | [0.064 ± 0.012] | [0.316 ± 0.043] | [0.16 ± 0.03] | 446,214 | 369 | 0 |
| B_smote | 0.166 ± 0.015 | 0.417 ± 0.007 | [0.277 ± 0.014] | [0.429 ± 0.035] | [0.68 ± 0.04] | 446,583 | 738 | 369 |
| C_adaptive | 0.163 ± 0.036 | 0.443 ± 0.005 | [0.679 ± 0.055] | [0.997 ± 0.006] | [0.98 ± 0.03] | 451,186 | 5,341 | 4,972 |
| amount floor | 0.057 ± 0.000 | 0.212 ± 0.000 | 0.006 ± 0.005 | 0.040 ± 0.012 | 0.02 ± 0.01 | — | — | — |

_mean ± sd over 3 seed(s) [1337, 7, 21]; **[brackets]** mark numbers the harness withholds; detector backend: lightgbm 4.5.0_

**The fold.** All three systems train on 446,214 rows (369 fraud) out of a 637,219-row pool, and
are scored on one test window: **unseen** 150,656 rows / 300 positives (base rate 0.1991%, all
injected), **known** 150,766 rows / 410 positives (410 of them the anchor's own labelled fraud).
Committed boundary `6ea0b94c1c8d3d3a`, embargo 90,000s, sample fraction 0.1.

| guard | what it checked | result |
| --- | --- | ---: |
| family carve-out | the training rows | 446,368 rows, 0 leaked |
| out-of-time embargo | committed gap 90,000s | actual gap 90,000s |
| haystack | legit rows in the test window | 150,356 of 150,356 kept |
| one haystack, two columns | legit rows shared by both columns | 150,356 shared |

**One contract field.** The worst single field on this anchor's `M3` rows is `hour_of_day`, at
PR-AUC 0.0031 against a base rate of 0.0005 — trivially separable: **False**.

**The provenance probe.** A classifier given the fold's own features sorts the injected `M3` rows
from this anchor's own traffic at PR-AUC **0.970** (3-fold cross-validated, 300 injected rows in
150,656) — separable: **True**. Every positive in the unseen column is injected and every negative
is real, so a probe at or above the detector's own score means that column cannot tell detection
apart from provenance.

**Against this anchor's own fraud.** The committed baseline reaches PR-AUC 0.152 on paysim's own
labelled fraud in the same test window (410 real positives, `artifacts/detector/paysim.json`),
where sorting by amount alone reaches 0.057. Any row above that line on the unseen column is
easier unseen than seen, which is a statement about the injected rows rather than a generalisation
result.

**System C's loop.** 12 rounds of 12 episodes, searching `S1`, `S2`, `S3` with the `search`
allocation. Every candidate batch is audit-gated on the `envelope` rule before the detector is
allowed to train on it, and a rejected batch is discarded rather than learned from — see
`afl/attack/multi.py:AUDIT_RULES` for what the two rules disagree about.

| seed | rounds | rejected | would reject: envelope / lift | rows kept | evasion first → last | min / max |
| --- | ---: | ---: | ---: | ---: | --- | --- |
| 1337 | 12 | 0 | 0 / 12 | 4,792 | 0.616 → 0.014 | 0.000 / 0.616 |
| 7 | 12 | 0 | 0 / 12 | 4,812 | 0.613 → 0.000 | 0.000 / 0.613 |
| 21 | 12 | 0 | 0 / 12 | 5,314 | 0.346 → 0.006 | 0.002 / 0.346 |

**Does provenance explain System C?** A model given the same training rows as System C and told
only *which rows the generator wrote* — never which rows are fraud, and never a row of the
held-out family — scores PR-AUC **0.863** on the held-out column against System C's **0.679**
(4,792 generated rows to learn from, separable: **True**). On this anchor provenance alone matches
or beats System C, so System C's held-out number is the generator's fingerprint transferring
between families rather than a detector generalising to an unseen attack.

**Every comparison, paired by seed.**

| comparison | column | metric | per-seed Δ | mean ± sd | wins | sign-test p | reading |
| --- | --- | --- | --- | --- | ---: | ---: | --- |
| C_adaptive − B_smote | known | pr_auc | +0.027, -0.006, -0.031 | -0.0032 ± 0.0290 | 1/3 | 0.875 | inside the spread |
| C_adaptive − A_baseline | known | pr_auc | +0.031, -0.002, -0.027 | +0.0010 ± 0.0290 | 1/3 | 0.875 | inside the spread |
| B_smote − A_baseline | known | pr_auc | +0.004, +0.004, +0.004 | +0.0042 ± 0.0003 | 3/3 | 0.125 | beats |
| C_adaptive − B_smote | known | recall_at_fixed_fpr | +0.024, +0.024, +0.029 | +0.0260 ± 0.0028 | 3/3 | 0.125 | beats |
| C_adaptive − A_baseline | known | recall_at_fixed_fpr | +0.005, +0.010, +0.002 | +0.0057 ± 0.0037 | 3/3 | 0.125 | beats |
| B_smote − A_baseline | known | recall_at_fixed_fpr | -0.020, -0.015, -0.027 | -0.0203 ± 0.0061 | 0/3 | 1.000 | loses |
| C_adaptive − B_smote | unseen | pr_auc | +0.438, +0.434, +0.333 | +0.4019 ± 0.0597 | 3/3 | 0.125 | beats, column withheld |
| C_adaptive − A_baseline | unseen | pr_auc | +0.665, +0.618, +0.561 | +0.6147 ± 0.0519 | 3/3 | 0.125 | beats, column withheld |
| B_smote − A_baseline | unseen | pr_auc | +0.227, +0.183, +0.228 | +0.2128 ± 0.0254 | 3/3 | 0.125 | beats, column withheld |
| C_adaptive − B_smote | unseen | recall_at_fixed_fpr | +0.537, +0.607, +0.560 | +0.5678 ± 0.0356 | 3/3 | 0.125 | beats, column withheld |
| C_adaptive − A_baseline | unseen | recall_at_fixed_fpr | +0.637, +0.720, +0.687 | +0.6811 ± 0.0419 | 3/3 | 0.125 | beats, column withheld |
| B_smote − A_baseline | unseen | recall_at_fixed_fpr | +0.100, +0.113, +0.127 | +0.1133 ± 0.0133 | 3/3 | 0.125 | beats, column withheld |

**Why it landed there.**

- **The held-out column cannot carry a claim on this anchor, so neither system's number on it is
  quotable and the difference between them is not either — a classifier sorts the injected M3 rows
  from this anchor's own traffic at PR-AUC 0.970, against 0.059 for the detector on the same rows —
  every positive here is injected and every negative is real, so the fold cannot tell detection
  apart from provenance.** Evidence: `{"outcome": "withheld", "cells_affected": "9/9"}`.
- **The attacker's evasion rate collapsed inside the loop, so the rows System C trained on are
  rows the detector had already learned to catch. A converged loop teaches the detector nothing new
  by definition.** Evidence: `{"evasion_first_round": 0.5251, "evasion_final_round": 0.0068}`.
- **A model that knows nothing except which rows the generator wrote reaches System C's held-out
  score on the same rows — so C's number on that column is the generator's fingerprint transferring
  between families, not a detector generalising to an unseen attack. Both models trained on the same
  injected rows; only one of them was told what fraud is.** Evidence: `{"system_c_pr_auc": 0.723904,
  "provenance_only_pr_auc": 0.998501, "trained_on": "the loop's own output, which is System C's
  training set"}`.

**Every seed.**

| seed | system | unseen PR-AUC | unseen rec@FPR | known PR-AUC | known rec@FPR | train rows | train fraud | generated |
| ---: | --- | ---: | ---: | ---: | ---: | ---: | ---: | ---: |
| 1337 | A_baseline | 0.059 | 0.363 | 0.158 | 0.444 | 446,214 | 369 | 0 |
| 1337 | B_smote | 0.285 | 0.463 | 0.162 | 0.424 | 446,583 | 738 | 369 |
| 1337 | C_adaptive | 0.724 | 1.000 | 0.189 | 0.449 | 451,006 | 5,161 | 4,792 |
| 7 | A_baseline | 0.078 | 0.280 | 0.179 | 0.432 | 446,214 | 369 | 0 |
| 7 | B_smote | 0.261 | 0.393 | 0.183 | 0.417 | 446,583 | 738 | 369 |
| 7 | C_adaptive | 0.695 | 1.000 | 0.177 | 0.441 | 451,026 | 5,181 | 4,812 |
| 21 | A_baseline | 0.056 | 0.303 | 0.148 | 0.437 | 446,214 | 369 | 0 |
| 21 | B_smote | 0.285 | 0.430 | 0.153 | 0.410 | 446,583 | 738 | 369 |
| 21 | C_adaptive | 0.618 | 0.990 | 0.122 | 0.439 | 451,528 | 5,683 | 5,314 |

_Runtime: seed 1337 433s, seed 7 512s, seed 21 542s._

## Where the numbers came from

Every cell here is read out of `artifacts/three_system/<anchor>.json` — the eval config as it was
read, the committed split digest, the seeds, per-column row counts, all four guards, the
commensurability audit, the provenance probe, the loop's per-round history and each system's
model card. Nothing in this document is typed by hand, and nothing in it can be refreshed without
re-running the command that produced the artefacts:

```bash
make table                                     # every anchor, every seed
python scripts/build_three_system.py paysim --seeds 1337 7
python scripts/build_three_system.py --doc-only    # rebuild this file from the artefacts
```
