# Exact versions, for environments without uv. `pyproject.toml` declares the ranges this
# project supports; this file records the one point inside them that produced the committed
# artefacts, because a range is not a reproduction: LightGBM 4.3 and 4.5 are both allowed by
# the range and are not guaranteed to give the same number.
#
# Keep in sync with `uv.lock` (which pins transitive dependencies too — prefer `uv sync`).
# `artifacts/reproduce/synthetic_headline.json` records the versions the expectation was
# recorded on; `make reproduce` compares them against yours and says so when they differ.
pydantic==2.13.4
numpy==2.0.2
pandas==2.2.3
scipy==1.14.1
scikit-learn==1.5.2
lightgbm==4.5.0
networkx==3.4.2
hydra-core==1.3.5
omegaconf==2.3.1
pyyaml==6.0.3
optuna==4.0.0
shap==0.46.0
mlflow==2.17.2
fastapi==0.115.14
uvicorn[standard]==0.32.1
streamlit==1.40.2
matplotlib==3.9.4
pyarrow==17.0.0
pytest==8.4.2
ruff==0.7.4
