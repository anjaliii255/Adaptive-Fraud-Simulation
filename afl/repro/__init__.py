"""The reproducibility instruments: what a number is, and what proves it.

Two questions this package answers, and nothing else:

  * `claims`   — does every number quoted in a document still fall out of the artefact it
                 names? A document is a cache of a computation, and caches go stale.
  * `runcard`  — which code, which seed, which machine produced an artefact? A result whose
                 provenance is remembered rather than recorded is a result nobody can re-run.
"""
